version: '3.8'

services:
  mvp:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - ../models:/models
      - ../data:/data
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
